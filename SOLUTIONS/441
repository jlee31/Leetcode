class Solution(object):
    def arrangeCoins(self, n):
        """
        :type n: int
        :rtype: int
        """
        rows = 0
        num = n

        for i in range(1,num+1):
            num -= i
            if num < 0:
                break
            rows += 1
        return rows
