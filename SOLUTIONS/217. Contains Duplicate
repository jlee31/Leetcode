class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        from collections import Counter
        counter = Counter(nums)
        for i in counter:
            if counter[i] > 1:
                return True
        return False
